# 贡献指南

## 核心哲学

本项目致力于维护一条**原子化、高信噪比**的 Git 主干历史（主干上每个提交对应一个完整特性）。同时，我们**拒绝“历史化妆术”**——开发过程中的关键决策与权衡不会被丢弃，而是通过**结构化文档**固化在各子项目的 `adr/` 目录中，实现“代码极简历史”与“设计丰富上下文”的分离。

## 一、PR 基础规范

### 1. 单一职责原则
- 每个PR必须仅包含**一个**独立的特性（Feature）、修复（Fix）或重构（Refactor）。
- **禁止**在特性PR中夹杂无关的格式化、拼写错误修正或依赖升级（除非该升级是该特性的前置硬性条件）。

### 2. 合并策略：Squash and Merge
- 所有PR在合并进主干时，**必须**选用 “Squash and Merge”（压缩合并）。
- 压缩后的提交信息必须符合 [Conventional Commits](https://www.conventionalcommits.org/) 规范，清晰概括该特性的全貌。

**目的**：确保 `git bisect` 能精准定位问题，且 `git revert` 能无损撤销整个PR。

## 二、关键信息文档化策略

为了弥补 Squash 合并丢失的开发上下文，贡献者**必须**根据特性复杂度，将决策过程沉淀为文档。

### 1. 何时必须创建文档？
以下情况，**必须**在对应子项目的 `adr/decisions/`（或 `adr/features/`）目录下新增或更新 Markdown 文档：
- 引入新的架构模式或外部依赖。
- 涉及Schema变更、API契约等Breaking Change修改。
- 存在**多种**可行技术方案，最终做出了取舍（Trade-off）。
- 该特性涉及跨模块联动，非显而易见的逻辑闭环。

> **例外**：简单的 Bug 修复、非公开函数逻辑微调、显而易见的函数封装，无需创建文档，但需在PR描述中简述意图。

### 2. 文档命名规范
- 格式：`<feature-name>.md`
- 示例：`api-rate-limiter.md`

### 3. 文档内容要求
文档应仅包含以下 4 个核心板块，严禁长篇大论的流水账：

- **背景**：为什么需要这个特性？
- **核心决策**：最终选择了什么方案？代码位于哪个模块？
- **权衡分析**：放弃了什么替代方案？为什么？
- **影响范围**：对现有 API、配置项等的潜在破坏性影响。

对应术语分别是Context, Decision, Trade-offs, Impact

## 三、PR 描述与文档引用规范

PR 描述**必须**保持简洁，严禁将上述文档内容大段复制粘贴到PR描述中。正确的引用方式如下：

### PR 描述模板

```md
## 简短摘要
<一句话说清楚这个PR做了什么>

## 关键信息索引
- **设计决策文档**：[`<子目录>/adr/decisions/xxx.md`](相对路径链接)
- **核心代码变更**：[核心逻辑实现](代码文件链接#L10-L30)
- **关联 Issues**：Closes #XXX
```

### 链接使用细则
- **引用文档**：使用仓库内**相对路径**（如 `<子目录>/adr/decisions/foo.md`）。
- **引用代码**：使用**代码链接 + 锚点**（如 `src/core.ts#L45-L60`），便于审阅者直接跳转，而非复制代码块。
- **严禁行为**：禁止在PR描述中粘贴大段异常堆栈、配置示例或设计文档原文。如需展示配置示例，请将配置文件放入 `examples/` 目录并链接之。

## 四、贡献者工作流

1. **开发前（可选但强烈建议）**：对于复杂特性，先提交一个仅包含设计文档的空壳 PR，待核心维护者确认方向后，再开始编码。
2. **本地开发**：在特性分支上，允许进行任意次提交。
3. **提交 PR**：确保PR标题符合规范，描述按上述模板填写。
4. **合并前自检**：
   - 运行 `git rebase main`，若存在多种可行的冲突处理方式，需在文档中补充冲突处理的权衡说明。
   - 确认 CI/CD 全量回归测试通过。
5. **合并**：由仓库维护者执行 **“Squash and Merge”**，并撰写高质量的最终提交信息，非新增子项目需包含 `Refs: <子目录>/adr/xxx.md`（如有）。

## 五、违反规则的后果

- **缺少必要文档**：维护者将要求补充文档，期间PR状态标记为 `awaiting-changes`。
- **PR 描述过载（复制大段原文）**：维护者将请求编辑PR描述以精简为链接形式，以减少邮件列表和通知区的噪音。
