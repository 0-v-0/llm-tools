# 项目文档格式规范

本规范约定本仓库内文档之间的引用方向，适用于仓库内所有文档。它与 [CONTRIBUTING.md](CONTRIBUTING.md) 的「关键信息文档化策略」互补：后者规定**何时**写文档、**写什么**；本规范规定文档之间**如何相互引用**。

## 核心规则

1. **设计文档不得引用实现文档**：设计文档不得出现指向实现文档或代码实现细节的引用。
2. **实现文档可以引用设计文档**：实现文档可以、且鼓励引用其所依据的设计文档或 ADR。

> 依赖方向只能由「实现」指向「设计」，反之禁止。

### 文档分类

| 类别 | 含义 | 本仓库中的位置 |
| --- | --- | --- |
| 设计文档 | 描述「做什么 / 为什么」的契约、架构与方案取舍 | 各子项目的 `adr/decisions/`、`adr/features/`，以及需求/契约类文档 |
| 实现文档 | 描述「怎么实现」的模块说明与代码组织 | 各子项目的 `docs/`，以及实现说明类文档 |

## 设计依据

本规则是「稳定依赖原则（Stable Dependencies Principle）」在文档层面的应用：

- **设计文档是稳定的**：它描述意图、契约与方案取舍，变更频率低，是其它文档与代码依赖的「锚点」。
- **实现文档是易变的**：它跟随代码重构、模块拆分而频繁变动。

若设计文档引用实现文档，则实现文档的每一次改动都会迫使设计文档同步更新，导致设计文档被实现细节「绑架」，失去其作为稳定上下文的定位。反向引用则将易变物锚定到稳定物，符合「依赖指向稳定性」的方向。

## 引用方向速查

| 引用方向 | 是否允许 | 说明 |
| --- | --- | --- |
| 实现文档 → 设计文档 | ✅ 允许 | 实现说明可链接其依据的设计/ADR |
| 设计文档 → 实现文档 | ❌ 禁止 | 设计文档不应耦合具体实现 |
| 设计文档 → 设计文档 | ✅ 允许 | 设计文档间可相互引用 |
| 实现文档 → 实现文档 | ✅ 允许 | 实现文档间可相互引用 |

判定要点：**被引用方是否比引用方更「稳定」**。若引用方比被引用方稳定（例如设计文档引用实现文档），则禁止。

## 修订历史管理

1. **修订历史集中存放**：每个文档的修订历史必须集中在文档内的单一章节（如「修订历史」），禁止将修订记录分散到文档各处。
2. **合并前整理归档**：PR 合并前，须将该文档的修订历史中值得长期保留的部分整理进 ADR 或其它长期存档位置，确保关键决策与变更背景不随修订历史删除而丢失。
3. **合并时删除**：PR 合并时删除文档内的修订历史章节，保持文档整洁；长期记录由 ADR 等存档位置承载。
